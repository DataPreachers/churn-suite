WITH CTE_EXPERIMENT_RAWDATA
AS
(
SELECT E.EXPERIMENT_ID, R.*
FROM EXPERIMENTS E
JOIN RAWDATA R ON R.FILE_ID = E.FILE_ID
	AND R.I_TIMEBASE BETWEEN E.BACKTEST_FROM AND E.BACKTEST_TO
)
SELECT * FROM CTE_EXPERIMENT_RAWDATA