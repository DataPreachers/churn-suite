WITH CTE_BACKTEST_COMBINED
AS
(
	SELECT KUNDE
	, CHURN_PROBABILITY
	, RISK_LEVEL
	, E.TRAINING_FROM
	, E.TRAINING_TO
	, E.BACKTEST_FROM
	, E.BACKTEST_TO
	, E.EXPERIMENT_ID
	FROM BACKTEST_RESULTS R
	JOIN EXPERIMENTS E ON E.EXPERIMENT_ID = R.ID_EXPERIMENTS
)
SELECT * FROM  CTE_BACKTEST_COMBINED
