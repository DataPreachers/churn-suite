WITH CTE_SCOPE
AS
(
	SELECT	  E.EXPERIMENT_ID
		    , MIN(I_TIMEBASE) AS BEGIN_SCOPE
		    , MAX(I_TIMEBASE) AS END_SCOPE
	FROM V_EXPERIMENT_RAWDATA R
	JOIN EXPERIMENTS E ON E.EXPERIMENT_ID = R.EXPERIMENT_ID
	GROUP BY E.EXPERIMENT_ID
)
SELECT * FROM CTE_SCOPE